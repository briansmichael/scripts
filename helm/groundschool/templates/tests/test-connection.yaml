apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "groundschool.fullname" . }}-test-connection"
  labels:
    {{- include "groundschool.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "groundschool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
